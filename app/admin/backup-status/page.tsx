'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Backups(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/backup-status',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.snapshots||[])})()},[]);return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / DATA SAFETY</small><h1>Backup Status</h1></div></div><div className="panel"><p><b>Important:</b> These are integrity/count snapshots, not full database backups. Enable Supabase backups/PITR separately for production.</p></div><div className="tableCard"><table><thead><tr><th>Status</th><th>Table Counts</th><th>Date</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.status}</td><td><code>{JSON.stringify(x.table_counts)}</code></td><td>{new Date(x.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></main>}
