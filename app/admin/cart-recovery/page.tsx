'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function CartRecovery(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/cart-recovery',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.carts||[])})()},[]);const value=rows.reduce((s,x)=>s+Number(x.cart_value||0),0);return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / RECOVERY</small><h1>Abandoned Carts</h1></div><b>${value.toFixed(2)} VALUE</b></div><div className="tableCard"><table><thead><tr><th>User</th><th>Items</th><th>Value</th><th>Recovery Sent</th><th>Recovered</th><th>Updated</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.user_id||'Guest'}</td><td>{x.item_count}</td><td>${Number(x.cart_value||0).toFixed(2)}</td><td>{x.recovery_sent_at?'Yes':'No'}</td><td>{x.recovered?'Yes':'No'}</td><td>{new Date(x.updated_at).toLocaleString()}</td></tr>)}</tbody></table></div></main>}
