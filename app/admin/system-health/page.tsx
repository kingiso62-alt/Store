'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function SystemHealth(){const [d,setD]=useState<any>(null);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/system-health',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setD(j)})()},[]);if(!d)return <main className="adminStandalone">Loading system health...</main>;const m=d.metrics;return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / HEALTH</small><h1>System Health</h1></div><b>{d.status.toUpperCase()}</b></div><div className="metricGrid"><div><small>PROVIDERS</small><b>{m.activeProviders}/{m.totalProviders}</b></div><div><small>OPEN ALERTS</small><b>{m.openAlerts}</b></div><div><small>TOP-UPS 24H</small><b>{m.topups24h}</b></div><div><small>TOP-UP FAILURES</small><b>{m.topupFailed24h}</b></div></div><div className="adminBottom"><div className="panel"><h3>Payments 24h</h3><b>{m.payments24h}</b></div><div className="panel"><h3>Payment Failures</h3><b>{m.paymentFailed24h}</b></div><div className="panel"><h3>Errors 24h</h3><b>{m.errors24h}</b></div></div></main>}
