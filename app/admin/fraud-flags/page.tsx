'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function FraudFlags(){const [rows,setRows]=useState<any[]>([]);async function token(){const {data}=await supabaseBrowser.auth.getSession();return data.session?.access_token||''}async function load(){const t=await token();const r=await fetch('/api/admin/fraud-flags',{headers:{authorization:`Bearer ${t}`}});const j=await r.json();if(r.ok)setRows(j.flags||[])}useEffect(()=>{load()},[]);async function set(id:string,status:string){const t=await token();await fetch(`/api/admin/fraud-flags/${id}`,{method:'PATCH',headers:{'content-type':'application/json',authorization:`Bearer ${t}`},body:JSON.stringify({status})});load()}return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / RISK</small><h1>Fraud Review</h1></div></div><div className="tableCard"><table><thead><tr><th>Customer</th><th>Reason</th><th>Risk</th><th>Status</th><th>Date</th><th>Review</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.profiles?.full_name||x.user_id}</td><td>{x.reason}</td><td>{x.risk_score}/100</td><td>{x.status}</td><td>{new Date(x.created_at).toLocaleString()}</td><td><button onClick={()=>set(x.id,'cleared')}>Clear</button> <button onClick={()=>set(x.id,'confirmed')}>Confirm</button></td></tr>)}</tbody></table></div></main>}
