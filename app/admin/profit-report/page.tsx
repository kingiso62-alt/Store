'use client';import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';export default function Profit(){const [d,setD]=useState<any>();useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/profit-report',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});if(r.ok)setD(await r.json())})()},[]);if(!d)return <main className="adminStandalone">Loading...</main>;return <main className="adminStandalone"><div className="pageHead"><div><small>LAST 30 DAYS</small><h1>Gross Profit</h1></div></div><div className="metricGrid"><div><small>REVENUE</small><b>${d.revenue.toFixed(2)}</b></div><div><small>COST</small><b>${d.cost.toFixed(2)}</b></div><div><small>GROSS PROFIT</small><b>${d.grossProfit.toFixed(2)}</b></div><div><small>MARGIN</small><b>{d.grossMargin.toFixed(1)}%</b></div></div></main>}