'use client';import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';export default function Alerts(){const [r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const q=await fetch('/api/admin/operations-alerts',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}}),j=await q.json();if(q.ok)setR(j.alerts||[])})()},[]);return <main className="adminStandalone"><div className="pageHead"><div><small>OPERATIONS</small><h1>Operations Alerts</h1></div><b>{r.filter(x=>!x.resolved).length} OPEN</b></div>{r.map(x=><div className="panel" key={x.id}><small>{x.source} · {x.severity}</small><h3>{x.title}</h3><p>{x.message}</p></div>)}</main>}