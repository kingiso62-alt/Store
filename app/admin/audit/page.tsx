'use client';import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Audit(){const [rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState('');useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/audit',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.logs||[]);else setMsg(j.error)})()},[]);return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / AUDIT</small><h1>Audit Log</h1></div></div>{msg&&<p>{msg}</p>}<div className="tableCard"><table><thead><tr><th>Action</th><th>Entity</th><th>Entity ID</th><th>Date</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.action}</td><td>{x.entity_type||'-'}</td><td>{x.entity_id||'-'}</td><td>{new Date(x.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></main>}
