'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function AdminRefunds(){const [rows,setRows]=useState<any[]>([]);async function token(){const {data}=await supabaseBrowser.auth.getSession();return data.session?.access_token||''}async function load(){const t=await token();const r=await fetch('/api/admin/refunds',{headers:{authorization:`Bearer ${t}`}});const j=await r.json();if(r.ok)setRows(j.refunds||[])}useEffect(()=>{load()},[]);async function set(id:string,status:string){const t=await token();await fetch(`/api/admin/refunds/${id}`,{method:'PATCH',headers:{'content-type':'application/json',authorization:`Bearer ${t}`},body:JSON.stringify({status})});load()}return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / FINANCE</small><h1>Refund Requests</h1></div></div><div className="tableCard"><table><thead><tr><th>Customer</th><th>Order</th><th>Amount</th><th>Reason</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.profiles?.full_name||'-'}</td><td>{x.orders?.order_number||'-'}</td><td>${Number(x.amount).toFixed(2)}</td><td>{x.reason}</td><td>{x.status}</td><td><button onClick={()=>set(x.id,'approved')}>Approve</button> <button onClick={()=>set(x.id,'rejected')}>Reject</button> <button onClick={()=>set(x.id,'refunded')}>Mark Refunded</button></td></tr>)}</tbody></table></div></main>}
