'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function NotificationsCenter(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/notifications-center',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setItems(j.items||[])})()},[]);return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / ALERTS</small><h1>Operations Notifications</h1></div><b>{items.length} ITEMS</b></div><div className="notificationStack">{items.map((x,i)=><div className="panel" key={`${x.source}-${x.id}-${i}`}><small>{x.source.toUpperCase()} · {x.severity||'info'}</small><h3>{x.title}</h3><p>{x.message}</p><small>{new Date(x.created_at).toLocaleString()}</small></div>)}</div></main>}
