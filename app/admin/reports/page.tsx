'use client';import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Reports(){const [data,setData]=useState<any>(null);useEffect(()=>{(async()=>{const {data:s}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/reports',{headers:{authorization:`Bearer ${s.session?.access_token||''}`}});const j=await r.json();if(r.ok)setData(j)})()},[]);if(!data)return <main className="adminStandalone">Loading reports...</main>;return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / REPORTS</small><h1>Reports</h1></div></div><div className="metricGrid"><div><small>REVENUE</small><b>${Number(data.summary.revenue).toFixed(2)}</b></div><div><small>ACCESSORIES</small><b>${Number(data.summary.physical).toFixed(2)}</b></div><div><small>TOP-UP</small><b>${Number(data.summary.digital).toFixed(2)}</b></div><div><small>ORDERS</small><b>{data.summary.orders}</b></div></div><div className="panel reportPanel"><h3>Last 30 Active Sales Days</h3><div className="reportBars">{data.daily.map((x:any)=><div key={x.date}><i style={{height:Math.max(8,Math.min(180,Number(x.total)))}}></i><small>{x.date.slice(5)}</small></div>)}</div></div></main>}
