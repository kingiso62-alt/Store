'use client';import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Sales(){const [r,setR]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession(),r=await fetch('/api/admin/sales-summary',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}}),j=await r.json();if(r.ok)setR(j.days||[])})()},[]);return <main className="adminStandalone"><div className="pageHead"><div><small>LAST 30 DAYS</small><h1>Daily Sales Summary</h1></div></div><div className="tableCard"><table><thead><tr><th>Date</th><th>Revenue</th><th>Orders</th><th>Top-Ups</th><th>Accessories</th></tr></thead><tbody>{r.map(x=><tr key={x.date}><td>{x.date}</td><td>${Number(x.revenue).toFixed(2)}</td><td>{x.orders}</td><td>{x.digital}</td><td>{x.physical}</td></tr>)}</tbody></table></div></main>}