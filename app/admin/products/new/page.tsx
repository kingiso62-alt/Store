'use client';import {FormEvent,useState} from 'react';
export default function NewProduct(){const [msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const name=String(f.get('name'));const body={name,slug:String(f.get('slug')||name.toLowerCase().replace(/[^a-z0-9]+/g,'-')),description:f.get('description'),productType:f.get('type'),price:f.get('price'),costPrice:f.get('cost')};const r=await fetch('/api/admin/products',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const j=await r.json();setMsg(r.ok?'Product created successfully.':j.error)}return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / PRODUCTS</small><h1>Add Product</h1></div></div><form className="adminForm" onSubmit={submit}><label>Product Name<input name="name" required/></label><label>Slug<input name="slug"/></label><div className="two"><label>Type<select name="type"><option value="physical">Physical Accessory</option><option value="digital">Digital Top-Up</option></select></label><label>Selling Price<input name="price" type="number" step="0.01" required/></label></div><label>Cost Price<input name="cost" type="number" step="0.01"/></label><label>Description<textarea name="description"/></label><div className="variantHint"><b>Variants & Images</b><p>Create the base product first. V5 database now supports multiple images and variants with SKU, price, stock and JSON attributes.</p></div><button className="btnBlue">SAVE PRODUCT</button>{msg&&<p>{msg}</p>}</form></main>}
