'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function CostAlerts(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/cost-alerts',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.alerts||[])})()},[]);return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / COSTS</small><h1>Supplier Cost Alerts</h1></div></div><div className="tableCard"><table><thead><tr><th>Product</th><th>Package</th><th>Provider</th><th>Old Cost</th><th>New Cost</th><th>Change</th><th>Date</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.topup_packages?.products?.name}</td><td>{x.topup_packages?.package_name}</td><td>{x.api_providers?.name}</td><td>${Number(x.old_cost).toFixed(2)}</td><td>${Number(x.new_cost).toFixed(2)}</td><td>{Number(x.change_percent).toFixed(1)}%</td><td>{new Date(x.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></main>}
