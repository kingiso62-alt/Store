'use client';import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function EnvCheck(){const [rows,setRows]=useState<any[]>([]),[ready,setReady]=useState(false);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/env-check',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok){setRows(j.environment||[]);setReady(j.ready)}})()},[]);return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / DEPLOYMENT</small><h1>Environment Check</h1></div><b>{ready?'Core Ready':'Missing Core Variables'}</b></div><div className="tableCard"><table><thead><tr><th>Variable</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.name}><td>{x.name}</td><td>{x.configured?'Configured':'Missing'}</td></tr>)}</tbody></table></div></main>}
