'use client';import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function ProviderHealth(){const [rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState('');useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/provider-health',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.providers||[]);else setMsg(j.error)})()},[]);return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / PROVIDERS</small><h1>Provider Health</h1></div></div>{msg&&<p>{msg}</p>}<div className="healthGrid">{rows.map(x=><article key={x.id}><h3>{x.name}</h3><p>{x.provider_type}</p><b className={`health ${x.health?.status||'unknown'}`}>{x.health?.status||'unknown'}</b><small>{x.health?.latency_ms?`${x.health.latency_ms} ms`:'No latency data'}</small></article>)}</div></main>}
