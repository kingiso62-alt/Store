'use client';import {FormEvent,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function AdminNotifications(){const [msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/notifications',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${data.session?.access_token||''}`},body:JSON.stringify({broadcast:true,title:f.get('title'),body:f.get('body'),type:'broadcast'})});const j=await r.json();setMsg(r.ok?`Sent to ${j.count} customers.`:j.error)}return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / NOTIFICATIONS</small><h1>Broadcast Notifications</h1></div></div><form className="adminForm" onSubmit={submit}><label>Title<input name="title" required/></label><label>Message<textarea name="body" required/></label><button className="btnBlue">SEND TO CUSTOMERS</button>{msg&&<p>{msg}</p>}</form></main>}
