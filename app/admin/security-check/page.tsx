'use client';import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function SecurityCheck(){const [data,setData]=useState<any>(null);useEffect(()=>{(async()=>{const {data:s}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/security-check',{headers:{authorization:`Bearer ${s.session?.access_token||''}`}});const j=await r.json();if(r.ok)setData(j)})()},[]);if(!data)return <main className="adminStandalone">Loading security check...</main>;return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / SECURITY</small><h1>Production Security Check</h1></div><b>{data.score}/{data.total}</b></div><div className="tableCard"><table><thead><tr><th>Check</th><th>Status</th></tr></thead><tbody>{data.checks.map((x:any)=><tr key={x.name}><td>{x.name}</td><td>{x.ok?'Ready':'Missing / Needs Attention'}</td></tr>)}</tbody></table></div></main>}
