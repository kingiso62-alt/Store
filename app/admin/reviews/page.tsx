'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Reviews(){const [rows,setRows]=useState<any[]>([]);async function token(){const {data}=await supabaseBrowser.auth.getSession();return data.session?.access_token||''}async function load(){const t=await token();const r=await fetch('/api/admin/reviews',{headers:{authorization:`Bearer ${t}`}});const j=await r.json();if(r.ok)setRows(j.reviews||[])}useEffect(()=>{load()},[]);async function set(id:string,status:string){const t=await token();await fetch('/api/admin/reviews',{method:'PATCH',headers:{'content-type':'application/json',authorization:`Bearer ${t}`},body:JSON.stringify({id,status})});load()}return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / REVIEWS</small><h1>Product Reviews</h1></div></div><div className="tableCard"><table><thead><tr><th>Product</th><th>Customer</th><th>Rating</th><th>Review</th><th>Status</th><th>Moderate</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.products?.name}</td><td>{x.profiles?.full_name||'-'}</td><td>{x.rating}/5</td><td>{x.title||x.body||'-'}</td><td>{x.status}</td><td><button onClick={()=>set(x.id,'approved')}>Approve</button> <button onClick={()=>set(x.id,'rejected')}>Reject</button></td></tr>)}</tbody></table></div></main>}
