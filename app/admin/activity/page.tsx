'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Activity(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/activity',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.activity||[])})()},[]);return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / AUDIT</small><h1>Activity Feed</h1></div><b>{rows.length} EVENTS</b></div><div className="tableCard"><table><thead><tr><th>Action</th><th>Entity</th><th>User</th><th>Details</th><th>Date</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.action}</td><td>{x.entity_type||'-'}</td><td>{x.user_id||'-'}</td><td><code>{JSON.stringify(x.metadata||{})}</code></td><td>{new Date(x.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></main>}
