'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function InventoryAlerts(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/inventory-alerts',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.items||[])})()},[]);const bad=rows.filter(x=>x.state!=='ok');return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / INVENTORY</small><h1>Inventory Alerts</h1></div><b>{bad.length} NEED ATTENTION</b></div><div className="tableCard"><table><thead><tr><th>Product</th><th>Stock</th><th>Threshold</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.stock}</td><td>{x.low_stock_threshold||5}</td><td>{x.state.replaceAll('_',' ')}</td></tr>)}</tbody></table></div></main>}
