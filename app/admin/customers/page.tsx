'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Customers(){const [rows,setRows]=useState<any[]>([]),[search,setSearch]=useState('');useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/customers',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.customers||[])})()},[]);const filtered=rows.filter(x=>(x.full_name||'').toLowerCase().includes(search.toLowerCase())||(x.phone||'').includes(search));return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / CUSTOMERS</small><h1>Customers</h1></div><input placeholder="Search name / phone" value={search} onChange={e=>setSearch(e.target.value)}/></div><div className="tableCard"><table><thead><tr><th>Name</th><th>Phone</th><th>Joined</th><th></th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td>{x.full_name||'-'}</td><td>{x.phone||'-'}</td><td>{new Date(x.created_at).toLocaleDateString()}</td><td><a href={`/admin/customer/${x.id}`}>View</a></td></tr>)}</tbody></table></div></main>}
