'use client';import {FormEvent,useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Brands(){const [rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState('');async function token(){const {data}=await supabaseBrowser.auth.getSession();return data.session?.access_token||''}async function load(){const t=await token();const r=await fetch('/api/admin/brands',{headers:{authorization:`Bearer ${t}`}});const j=await r.json();if(r.ok)setRows(j.brands||[]);else setMsg(j.error)}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),t=await token();const r=await fetch('/api/admin/brands',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${t}`},body:JSON.stringify({name:f.get('name'),logoUrl:f.get('logoUrl')})});const j=await r.json();setMsg(r.ok?'Brand added.':j.error);if(r.ok){e.currentTarget.reset();load()}}async function edit(x:any){const name=prompt('New name',x.name);if(!name)return;const t=await token();const body={name};await fetch(`/api/admin/brands/${x.id}`,{method:'PATCH',headers:{'content-type':'application/json',authorization:`Bearer ${t}`},body:JSON.stringify(body)});load()}async function del(id:string){if(!confirm('Delete item?'))return;const t=await token();await fetch(`/api/admin/brands/${id}`,{method:'DELETE',headers:{authorization:`Bearer ${t}`}});load()}return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / BRANDS</small><h1>Brands</h1></div></div><div className="providerLayout"><form className="adminForm" onSubmit={submit}><label>Name<input name="name" required/></label><label>Logo URL<input name="logoUrl"/></label><button className="btnBlue">ADD BRAND</button>{msg&&<p>{msg}</p>}</form><div className="tableCard"><table><thead><tr><th>Name</th><th>Logo</th><th>Action</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.logo_url||'-'}</td><td><button onClick={()=>edit(x)}>Edit</button> <button onClick={()=>del(x.id)}>Delete</button></td></tr>)}</tbody></table></div></div></main>}
