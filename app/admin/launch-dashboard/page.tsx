'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function LaunchDashboard(){const [data,setData]=useState<any>(null);useEffect(()=>{(async()=>{const {data:s}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/launch-dashboard',{headers:{authorization:`Bearer ${s.session?.access_token||''}`}});const j=await r.json();if(r.ok)setData(j)})()},[]);if(!data)return <main className="adminStandalone">Loading launch dashboard...</main>;const m=data.metrics;return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / LAUNCH</small><h1>Launch Control Center</h1></div><b>{data.launchReady?'READY':'NOT READY'}</b></div><div className="metricGrid"><div><small>ACTIVE PROVIDERS</small><b>{m.activeProviders}/{m.totalProviders}</b></div><div><small>OPEN ALERTS</small><b>{m.openProviderAlerts}</b></div><div><small>ACTIVE PRODUCTS</small><b>{m.activeProducts}</b></div><div><small>PAYMENT METHODS</small><b>{m.activePaymentMethods}</b></div></div><div className="adminBottom"><div className="panel"><h3>Sandbox Passed</h3><b>{m.recentSandboxPassed}</b></div><div className="panel"><h3>Sandbox Failed</h3><b>{m.recentSandboxFailed}</b></div><div className="panel"><h3>Launch State</h3><b>{data.launchReady?'GO':'HOLD'}</b></div></div></main>}
