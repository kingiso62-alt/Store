'use client';import {FormEvent,useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function PaymentMethods(){const [rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState('');async function token(){const {data}=await supabaseBrowser.auth.getSession();return data.session?.access_token||''}async function load(){const t=await token();const r=await fetch('/api/admin/payment-methods',{headers:{authorization:`Bearer ${t}`}});const j=await r.json();if(r.ok)setRows(j.methods||[]);else setMsg(j.error)}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),t=await token();const r=await fetch('/api/admin/payment-methods',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${t}`},body:JSON.stringify({code:f.get('code'),name:f.get('name'),sortOrder:f.get('sortOrder'),isActive:true})});const j=await r.json();setMsg(r.ok?'Payment method saved.':j.error);if(r.ok){e.currentTarget.reset();load()}}async function toggle(x:any){const t=await token();await fetch(`/api/admin/payment-methods/${x.id}`,{method:'PATCH',headers:{'content-type':'application/json',authorization:`Bearer ${t}`},body:JSON.stringify({is_active:!x.is_active})});load()}return <main className="adminStandalone"><div className="pageHead"><div><small>ADMIN / PAYMENTS</small><h1>Payment Methods</h1></div></div><div className="providerLayout"><form className="adminForm" onSubmit={submit}><label>Code<input name="code" placeholder="evc" required/></label><label>Name<input name="name" placeholder="EVC Plus" required/></label><label>Sort Order<input name="sortOrder" type="number" defaultValue="0"/></label><button className="btnBlue">SAVE METHOD</button>{msg&&<p>{msg}</p>}</form><div className="tableCard"><table><thead><tr><th>Name</th><th>Code</th><th>Status</th><th>Action</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.code}</td><td>{x.is_active?'Active':'Disabled'}</td><td><button onClick={()=>toggle(x)}>{x.is_active?'Disable':'Enable'}</button></td></tr>)}</tbody></table></div></div></main>}
