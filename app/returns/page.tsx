'use client';import {FormEvent,useState} from 'react';import Header from '../../components/Header';import Footer from '../../components/Footer';
export default function Returns(){const [msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/returns',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({orderNumber:f.get('orderNumber'),reason:f.get('reason'),details:f.get('details')})});const j=await r.json();setMsg(r.ok?'Return request submitted.':j.error)}return <><Header/><main className="wrap authPage"><form className="authCard" onSubmit={submit}><h1>Return Request</h1><p>Request a return for an eligible physical order.</p><label>Order Number<input name="orderNumber" required/></label><label>Reason<select name="reason"><option>Damaged</option><option>Wrong Product</option><option>Not Working</option><option>Other</option></select></label><label>Details<textarea name="details"/></label><button className="btnBlue full">SUBMIT REQUEST</button>{msg&&<small className="authMsg">{msg}</small>}</form></main><Footer/></>}
