'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Orders(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/account/orders',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.orders||[])})()},[]);return <main className="wrap accountPage"><div className="pageHead"><div><small>MY ACCOUNT</small><h1>My Orders</h1></div></div><div className="orderCards">{rows.map(x=><a className="panel" key={x.id} href={`/account/orders/${x.id}`}><small>{x.order_number} · {new Date(x.created_at).toLocaleString()}</small><h3>{x.order_type==='digital'?'Digital Top-Up':'Gaming Accessories'}</h3><p>Status: <b>{x.status}</b></p><b>${Number(x.total).toFixed(2)}</b></a>)}</div></main>}
