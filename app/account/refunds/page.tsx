'use client';
import {FormEvent,useEffect,useState} from 'react';import {supabaseBrowser} from '../../../lib/supabase-browser';
export default function Refunds(){const [rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState('');async function token(){const {data}=await supabaseBrowser.auth.getSession();return data.session?.access_token||''}async function load(){const t=await token();const r=await fetch('/api/account/refunds',{headers:{authorization:`Bearer ${t}`}});const j=await r.json();if(r.ok)setRows(j.refunds||[])}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),t=await token();const r=await fetch('/api/account/refunds',{method:'POST',headers:{'content-type':'application/json',authorization:`Bearer ${t}`},body:JSON.stringify({orderId:f.get('orderId'),amount:f.get('amount'),reason:f.get('reason')})});const j=await r.json();setMsg(r.ok?'Refund request submitted.':j.error);if(r.ok){e.currentTarget.reset();load()}}return <main className="wrap accountPage"><div className="pageHead"><div><small>MY ACCOUNT</small><h1>Refunds</h1></div></div><div className="accountTwoCol"><form className="adminForm" onSubmit={submit}><label>Order ID<input name="orderId" required/></label><label>Amount<input name="amount" type="number" step="0.01"/></label><label>Reason<textarea name="reason" required/></label><button className="btnBlue">REQUEST REFUND</button>{msg&&<p>{msg}</p>}</form><div className="notificationStack">{rows.map(x=><div className="panel" key={x.id}><small>{x.orders?.order_number||x.order_id}</small><h3>${Number(x.amount).toFixed(2)}</h3><p>{x.reason}</p><b>{x.status}</b></div>)}</div></div></main>}
