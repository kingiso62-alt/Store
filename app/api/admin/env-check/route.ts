import {NextResponse} from 'next/server';import {requireAdmin} from '../../../../lib/server/require-admin';
export async function GET(req:Request){const g=await requireAdmin(req);if(g.error)return g.error;const names=['NEXT_PUBLIC_SUPABASE_URL','NEXT_PUBLIC_SUPABASE_ANON_KEY','SUPABASE_SERVICE_ROLE_KEY','NEXT_PUBLIC_SITE_URL','INTERNAL_JOB_SECRET','CRON_SECRET','MERCHANT_API_BASE_URL','MERCHANT_API_KEY','MERCHANT_WEBHOOK_SECRET','TOPUP_API_BASE_URL','TOPUP_API_KEY'];const result=names.map(name=>({name,configured:!!process.env[name]}));return NextResponse.json({environment:result,ready:result.filter(x=>!x.name.startsWith('MERCHANT_')&&!x.name.startsWith('TOPUP_')).every(x=>x.configured)});}
