import {NextResponse} from 'next/server';import {requirePermission} from '../../../../lib/server/require-permission';import {supabaseAdmin} from '../../../../lib/server/supabase-admin';
export async function GET(req:Request){const gate=await requirePermission(req,'view_orders');if(gate.error)return gate.error;const {data,error}=await supabaseAdmin().from('orders').select('id,order_number,order_type,status,total,created_at,user_id').order('created_at',{ascending:false}).limit(200);return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({orders:data||[]});}
