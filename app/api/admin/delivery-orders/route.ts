import {NextResponse} from 'next/server';import {requirePermission} from '../../../../lib/server/require-permission';import {supabaseAdmin} from '../../../../lib/server/supabase-admin';
export async function GET(req:Request){const g=await requirePermission(req,'manage_orders');if(g.error)return g.error;let q=supabaseAdmin().from('orders').select('id,order_number,status,total,user_id,created_at,delivery_status,delivery_fee,tracking_code,courier_name').eq('order_type','physical').order('created_at',{ascending:false}).limit(500);const {data,error}=await q;return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({orders:data||[]})}