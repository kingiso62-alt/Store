import {NextResponse} from 'next/server';import {requirePermission} from '../../../../../lib/server/require-permission';import {supabaseAdmin} from '../../../../../lib/server/supabase-admin';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const g=await requirePermission(req,'manage_orders');if(g.error)return g.error;const {id}=await params,b=await req.json();const patch:any={updated_at:new Date().toISOString()};if(b.status)patch.status=b.status;if(b.priority)patch.priority=b.priority;if(b.adminNote!==undefined)patch.admin_note=b.adminNote;const {data,error}=await supabaseAdmin().from('support_tickets').update(patch).eq('id',id).select().single();return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({ticket:data})}
