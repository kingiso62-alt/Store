'use client';
import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../lib/supabase-browser';
export default function RecentlyViewed(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/account/recently-viewed',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.items||[])})()},[]);return <main className="wrap"><div className="pageHead"><div><small>MY ACCOUNT</small><h1>Recently Viewed</h1></div></div><div className="productGrid">{rows.map(x=><a className="productCard" key={x.id} href={x.products.product_type==='digital'?`/topup/${x.products.slug}`:`/product/${x.products.slug}`}><h3>{x.products.name}</h3><b>${Number(x.products.price).toFixed(2)}</b><small>{new Date(x.viewed_at).toLocaleString()}</small></a>)}</div></main>}
