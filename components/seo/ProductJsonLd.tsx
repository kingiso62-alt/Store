export default function ProductJsonLd({product}:{product:any}){const image=product.product_images?.find((x:any)=>x.is_primary)?.url||product.product_images?.[0]?.url;const data={'@context':'https://schema.org','@type':'Product',name:product.name,description:product.description||undefined,image:image?[image]:undefined,brand:product.brands?.name?{'@type':'Brand',name:product.brands.name}:undefined,offers:{'@type':'Offer',priceCurrency:'USD',price:String(product.price),availability:(product.product_variants||[]).some((v:any)=>Number(v.stock)>0)?'https://schema.org/InStock':'https://schema.org/OutOfStock'}};return <script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(data)}}/>}
