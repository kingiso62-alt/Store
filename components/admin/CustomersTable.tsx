'use client';import {useEffect,useState} from 'react';import {supabaseBrowser} from '../../lib/supabase-browser';
export default function CustomersTable(){const [rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState('');useEffect(()=>{(async()=>{const {data}=await supabaseBrowser.auth.getSession();const r=await fetch('/api/admin/customers',{headers:{authorization:`Bearer ${data.session?.access_token||''}`}});const j=await r.json();if(r.ok)setRows(j.customers||[]);else setMsg(j.error)})()},[]);return <>{msg&&<p>{msg}</p>}<div className="tableCard"><table><thead><tr><th>Name</th><th>Phone</th><th>Orders</th><th>Total Spend</th><th>Joined</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.full_name||'Customer'}</td><td>{x.phone||'-'}</td><td>{x.orderCount}</td><td>${Number(x.totalSpend).toFixed(2)}</td><td>{new Date(x.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></>}
